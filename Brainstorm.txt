Shit to add:


task recipes in crop labour wizard
[
    I want to have a concept of different task recipes, and execute it similar
    to how the dailywizard subtool does.  The TASK_DEFS array can be saved
    to a .json file for any recipe.  The only tasks that are mandatory and default
    should be the anchor tasks that affect the other dates. Task lists
    can be saved to their own .json files, and imported into the main DB.
    
    Then, I want to design it so recipes can be made in the wizard and saved
    to the wizards database key the same way dailywizard does it.
    The main tasks that every crop will have
    will be a planting, a flip, and a harvest, with trim being optional.
    All other tasks can be moved to the user provided task list.

    Doing this will allow me to keep our proprietary tasks seperated from
    the suite, and allows us to use different techniques easily without
    adjusting the html.

    It's important the exact default recipe we are currently using for
    8 week and 9 week harvests get made into a json. You can just copy the
    array we use before implementing into its own .json to reinject and test.
    you can keep it in the same folder as the html for now, as its just
    temporary and for testing but dont put code in around auto loading adjacent
    jsons.  This is currently the tasks we use for crop projection.

    We have to design this not to break compatability.  The main sitkatools 
    calendar has a filtering feature that looks for the tags produced by
    croplabourwizard.  We will need to ensure the filtering system still works.

    The dailywizard tool should serve as a decent example of how we could save task recipes
    to the .json key for the subtool.  Feel free to write any standards into the Agent.MDs
    for both apps to keep things consistent across the suite.

    Let's move things over in steps and stages and stop to let me test things between
    each step, and lets develop a solid plan here first before we do this transition.
    Ask me any questions you need to make sure this is implemented correctly.
]

Employee requirements.  Put a "Estimated time" for events
based on the room they are in.  This is a number
ill just set and bake in some defaults

because capturing the info will take more time than wanted.
Leave room for a true captured number to represent instead
if it is available or toggled

-make some kind of graphs for money
-seperate employee wages to a secure seperate file
